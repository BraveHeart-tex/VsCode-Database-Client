<section class="query-view" aria-labelledby="query-view-title">
  <h1 id="query-view-title">Query</h1>
  <p>Select or connect to a database, then run SQL here.</p>
</section>

<style>
  .query-view {
    display: grid;
    gap: 8px;
    width: min(100%, 760px);
    padding: 24px;
    box-sizing: border-box;
    color: var(--vscode-foreground);
  }

  h1,
  p {
    margin: 0;
  }

  h1 {
    font-size: 22px;
    font-weight: 600;
    line-height: 1.25;
  }

  p {
    color: var(--vscode-descriptionForeground);
  }
</style>
